import { bootstrap }    from '@angular/platform-browser-dynamic';
import { AppComponent } from './app.component'; // This allows us to get access to a class that is present in a different file.

bootstrap(AppComponent);
